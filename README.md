# aws-utils
Helping scripts for aws functionaries.

To create ssh config for all instances in aws:
    python ssh_config.py > config

If populate using private ip:
    python ssh_config.py --private > config
